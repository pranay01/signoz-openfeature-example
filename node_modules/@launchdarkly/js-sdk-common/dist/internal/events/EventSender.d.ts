import { LDEventSender, LDEventSenderResult, LDEventType } from '../../api/subsystem';
import { ClientContext } from '../../options';
export default class EventSender implements LDEventSender {
    private crypto;
    private defaultHeaders;
    private diagnosticEventsUri;
    private eventsUri;
    private requests;
    constructor(clientContext: ClientContext);
    private tryPostingEvents;
    sendEventData(type: LDEventType, data: any): Promise<LDEventSenderResult>;
}
//# sourceMappingURL=EventSender.d.ts.map